---
name: missing-role-test
palette:
  base: "#000000"
  text: "#ffffff"

roles:
  body:
    fg: text
    bg: base
  # severity is intentionally absent — compile must reject because
  # severity.critical references an empty palette key.
