# Build artifacts
/a10r
/smoke
/dist/
coverage.out
*.test
*.prof

# Go workspace files (not used in this single-module repo)
go.work
go.work.sum

# Editor / IDE
/.idea/
/.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Per-user Claude Code overrides (the team-shared bits under
# .claude/ — settings.json, agents/ — are tracked).
.claude/settings.local.json
